describe("Wisher", function() {
  
  beforeEach(function() {
    
  });

  it("should be empty when created", function() {
    //GIVEN
    //WHEN
    var theWisher = new Wisher();
    //THEN
    var thisResult = theWisher.getJSON();
    var expectedResult = {};
    expect(thisResult).toEqual(expectedResult);
  });

  it("should be possible to invite friends", function() {
    //GIVEN
    //WHEN
    //THEN
    var thisResult = {state : "Note Implemented"};
    var expectedResult = {};
    expect(thisResult).toEqual(expectedResult);
  });

it("", function() {
    //GIVEN
    //WHEN
    //THEN
    var thisResult = {state : "Note Implemented"};
    var expectedResult = {};
    expect(thisResult).toEqual(expectedResult);
  });

});

 